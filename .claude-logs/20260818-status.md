# status 2026-08-18

## where things stand

- suite: 141 passed / 1 skipped (the old motor test), lint + typecheck green via `npm run check`
- book VII (advanced experiments) now runs in the simulator:
  - capacitor support: internal caps behind the CAPACITOR jacks (`1cap`-`6cap` notation,
    500uF sections 1-5 / 1000uF section 6), backward-euler companion model, `stepTime(ms)`
    advances charge; `addExternalResistor()` for the kit resistors
  - exp 1 simple flip-flop: works, matches the 6-step procedure incl. too-fast misses
  - exp 2 fast flip-flop (biased relay): works, incl. the speed contrast with exp 1;
    bias 220 ohm sits in the sim relay's 135-385 ohm window (pickup 70mA / dropout 30mA)
  - exp 3 oscillator: free-runs at ~24ms period (sec 1), ~2.2x slower on sec 6, resistor
    value changes frequency — all as the manual claims
- manual pages 8-9 document per-relay bias calibration = 1961 corroboration of the
  per-relay pickup variance hypothesis (from the 3-coils vs warm-bulb measurement conflict)

## unpushed commits (5)

19fa153 capacitor support + simple flip-flop, a0e4941 timeouts, 80c5abb fast flip-flop,
b3b3f0e timeouts, 575b359 oscillator — push deploys via github pages action

## next steps

- push (deploys)
- UI for book VII: CAPACITOR jacks on the panel, a way to place kit resistors, and the
  RAF loop driving `stepTime` so caps/oscillators run live in the browser
- book VII exp 4+ (oscillator-driven circuits, exp 2+3 combined for automatic operation)
- still open: book IV single-input flip-flop (SIMULATION-ERRATA.md) — needs wiring on
  the real device; sim behavior unchanged by all model improvements, unexplained
- mobile long-press = shift-click equivalent (TODO.md)
- flaky note: two unnamed test failures observed 2026-08-18 before timeout raises,
  not reproduced in 4+ clean runs since; if it recurs, capture the test name first
